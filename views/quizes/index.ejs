
    <table>
        <% for (var i=0; i < quizes.length; i++) { %>
        <tr>
            <td><%= i + 1 %></td>
            <td class="pregunta"><a href="/quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a></td>
        </tr>
        <% } %>
    </table>